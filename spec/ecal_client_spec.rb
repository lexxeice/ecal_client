require 'spec_helper'

describe EcalClient do
  it 'has a version number' do
    expect(EcalClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
